EDIT SECTION — SESSION NOTE
===========================

DONE:
1. Editor split into 3 files (in this "New folder"): editor.html = UI/design shell, renderer.js = draws slide on canvas, bridge-core.js = engine/brain (parse, colour, SVG rasterize, custGeom, OOXML edit). One-line links; brain stays untouched during UI work.
2. Import works: text, images (PNG/JPG/SVG→rasterized), shapes, groups, tables, connectors, custGeom; chart/SmartArt shown as placeholder. Edit text/move/resize → "Save PPTX" round-trips clean in PowerPoint / Google Slides / Keynote (proven).
3. Canva breaks SVG-as-fill (svgBlip) → solved two ways: SVG→native vector shapes (editable everywhere; Python converter v3 handles paths+primitives+strokes+gradient opacity) OR SVG→PNG (perfect look, flat). Also PDF/PNG export via LibreOffice. Key facts: PNG=universal but not editable; vector=editable + never tears; Canva caps loose image uploads <1440px (deliver as .pptx, not loose PNG).

NEXT:
4. IN PROGRESS: "Building the Future" deck — our editor DISPLAY drops some images. Finding: save is fine (media 117=117, XML pass-through keeps everything); the gap is DISPLAY — images/SVGs nested inside grpSp groups (7 groups on slide 1) not rendering. FIX: make renderer.js draw images inside groups + verify group transforms.
5. THEN: port SVG→PNG/vector converter into live editor export (currently Python), add master-slide flatten+strip for Canva, converter v4 (even-odd holes), gradient preview on canvas, resolution presets (Light/Standard/High), build export menu buttons (PPTX / Canva / PDF / PNG / Figma).
